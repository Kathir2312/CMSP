export interface IOpenings {
    value: IOpening[];
}

export interface IOpening {
    ID: number,
    Title: String,
    Opening : String,
    Qualification: String,
    StartDate: String,
    EndDate: String
}